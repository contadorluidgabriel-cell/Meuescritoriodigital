name: Validate V12.1

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Enable pnpm
        run: |
          corepack enable
          corepack prepare pnpm@11.19.0 --activate

      - name: Restore original lockfile
        run: node scripts/restore-lockfile.mjs

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build patched production source
        run: pnpm build

      - name: Regression tests on patched source
        run: pnpm test

      - name: Verify legacy bundle
        run: test -s dist/legacy-v10-7.html
